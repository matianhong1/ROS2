# 第2章 话题与消息

> 适用环境：Ubuntu 24.04 + ROS 2 Jazzy Jalisco  
> 学习方式：恢复环境 → 观察话题 → 分析消息 → 手动发布 → 测量频率  
> 前置知识：已完成第1章“ROS 2基础与计算图”

---

## 2.1 本章学习目标

完成本章后，应能够：

1. 说明发布者、订阅者、话题和消息之间的关系。
2. 区分话题名称、消息类型与实际消息数据。
3. 使用 `ros2 topic list -t` 查看话题及其消息类型。
4. 使用 `ros2 topic info` 查看发布者和订阅者数量。
5. 使用 `ros2 interface show` 查看消息的数据结构。
6. 使用 `ros2 topic echo` 实时观察话题数据。
7. 使用 `ros2 topic pub` 从终端主动发布消息。
8. 使用 `ros2 topic hz` 观察话题的发布频率。
9. 从自动控制系统框图的角度理解 ROS 2话题通信。

---

## 2.2 每次开机后的环境恢复

关闭虚拟机不会删除 Ubuntu、ROS 2或之前安装的软件。重新开机后，只需要重新启动本次实验需要的节点。

### 2.2.1 启动虚拟机

1. 打开 Windows 中的 Oracle VirtualBox。
2. 选中虚拟机 `Ubuntu-24.04-ROS2`。
3. 点击“启动”。
4. 等待 Ubuntu 登录界面出现并输入密码。

### 2.2.2 打开终端

进入 Ubuntu 桌面后，可以使用快捷键：

```text
Ctrl + Alt + T
```

打开终端。

终端中的复制、粘贴快捷键为：

```text
复制：Ctrl + Shift + C
粘贴：Ctrl + Shift + V
```

### 2.2.3 检查 ROS 2环境

打开终端后执行：

```bash
echo $ROS_DISTRO
```

正常输出：

```text
jazzy
```

这说明 ROS 2环境已经通过 `~/.bashrc`自动加载，不需要每次手动执行 `source`。

如果没有输出，可临时执行：

```bash
source /opt/ros/jazzy/setup.bash
```

---

## 2.3 话题通信的基本结构

话题（Topic）是 ROS 2节点之间传输数据的一种通信机制，适合连续、重复地传输数据。

典型结构为：

```text
发布者节点  --发布消息-->  话题  --传递消息-->  订阅者节点
```

在小海龟系统中：

```text
/teleop_turtle  -->  /turtle1/cmd_vel  -->  /turtlesim
```

对应关系如下：

| 项目 | 小海龟示例 | 自动控制类比 |
| --- | --- | --- |
| 发布者节点 | `/teleop_turtle` | 控制器或信号源 |
| 话题 | `/turtle1/cmd_vel` | 框图中带名称的信号线 |
| 订阅者节点 | `/turtlesim` | 被控对象或后续环节 |
| 消息类型 | `geometry_msgs/msg/Twist` | 信号向量的结构定义 |
| 实际消息 | 具体的线速度、角速度数值 | 某一时刻的控制量 `u(t)` |

### 2.3.1 发布者 Publisher

发布者负责产生消息并把消息发送到指定话题。

键盘控制节点读取方向键后，会把速度指令发布到：

```text
/turtle1/cmd_vel
```

### 2.3.2 订阅者 Subscriber

订阅者监听指定话题。当话题上出现新消息时，订阅者会接收并处理消息。

海龟仿真节点订阅 `/turtle1/cmd_vel`，收到速度指令后更新海龟的运动状态。

### 2.3.3 话题 Topic

话题是具有名称的数据通道。它不是节点，也不是某一条具体数据。

例如：

```text
/turtle1/cmd_vel
```

这个名称表达了该通道用于传输 `turtle1`的速度命令。

### 2.3.4 消息 Message

消息是话题中真正流动的数据。每个话题都要求发布者和订阅者使用兼容的消息类型。

必须区分下面三个概念：

| 概念 | 示例 | 含义 |
| --- | --- | --- |
| 话题名称 | `/turtle1/cmd_vel` | 数据从哪条通道传输 |
| 消息类型 | `geometry_msgs/msg/Twist` | 数据应具有怎样的结构 |
| 消息数据 | `linear.x: 2.0` | 本次实际传输了什么数值 |

---

## 2.4 实验准备：启动小海龟

本章多数实验需要至少运行海龟仿真节点。

### 2.4.1 终端1：启动海龟仿真节点

```bash
ros2 run turtlesim turtlesim_node
```

正常现象：弹出小海龟窗口。

如果希望继续使用第1章练习过的节点名称，可以执行：

```bash
ros2 run turtlesim turtlesim_node --ros-args --remap __node:=car_simulator
```

两种启动方法任选其一。后续笔记默认使用原始节点名 `/turtlesim`，但节点名称的变化不影响话题实验。

### 2.4.2 终端2：启动键盘控制节点

```bash
ros2 run turtlesim turtle_teleop_key
```

保持终端2处于选中状态，方向键才会控制海龟。

### 2.4.3 终端3：执行观察命令

再打开一个终端，用它执行本章中的 `ros2 topic`和 `ros2 interface`命令。

---

## 2.5 查看当前话题

### 2.5.1 只查看话题名称

在终端3执行：

```bash
ros2 topic list
```

可能看到：

```text
/parameter_events
/rosout
/turtle1/cmd_vel
/turtle1/color_sensor
/turtle1/pose
```

### 2.5.2 同时查看话题名称与消息类型

```bash
ros2 topic list -t
```

关键输出示例：

```text
/turtle1/cmd_vel [geometry_msgs/msg/Twist]
/turtle1/color_sensor [turtlesim/msg/Color]
/turtle1/pose [turtlesim/msg/Pose]
```

其中：

- 方括号前面是话题名称。
- 方括号里面是消息类型。
- `msg`表示消息接口类别，不是完整的消息类型名称。

例如：

```text
geometry_msgs / msg / Twist
功能包名称       类别    消息名称
```

---

## 2.6 查看话题的连接信息

执行：

```bash
ros2 topic info /turtle1/cmd_vel
```

可能输出：

```text
Type: geometry_msgs/msg/Twist
Publisher count: 1
Subscription count: 1
```

含义如下：

| 输出 | 含义 |
| --- | --- |
| `Type` | 该话题使用的消息类型 |
| `Publisher count` | 当前有多少个发布者向它发送消息 |
| `Subscription count` | 当前有多少个订阅者接收它的消息 |

在本实验中：

- `/teleop_turtle`是一个发布者。
- `/turtlesim`是一个订阅者。

如果停止键盘节点，发布者数量通常会变为 `0`；如果停止海龟节点，订阅者数量通常会变为 `0`。

查看更详细的端点和 QoS信息：

```bash
ros2 topic info /turtle1/cmd_vel --verbose
```

QoS将在后续章节系统学习，本章只观察，不要求记忆。

---

## 2.7 查看消息类型的数据结构

知道消息类型为 `geometry_msgs/msg/Twist`后，可以继续查看它内部有哪些字段：

```bash
ros2 interface show geometry_msgs/msg/Twist
```

核心结构为：

```text
Vector3  linear
Vector3  angular
```

`linear`和 `angular`本身又都是三维向量。继续查看 `Vector3`：

```bash
ros2 interface show geometry_msgs/msg/Vector3
```

可以看到：

```text
float64 x
float64 y
float64 z
```

因此 `Twist`完整地包含六个数值：

```text
linear.x
linear.y
linear.z
angular.x
angular.y
angular.z
```

### 2.7.1 小海龟实际使用的字段

小海龟在二维平面内运动，主要使用：

| 字段 | 含义 |
| --- | --- |
| `linear.x` | 前进或后退的线速度 |
| `angular.z` | 绕竖直方向旋转的角速度 |

其他字段通常保持为 `0.0`。

从控制系统角度看，可以将控制输入写成：

$$
u(t)=\begin{bmatrix}v(t)\\\omega(t)\end{bmatrix}
$$

其中：

- $v(t)$对应 `linear.x`。
- $\omega(t)$对应 `angular.z`。

---

## 2.8 实时观察话题数据

### 2.8.1 观察速度命令

在终端3执行：

```bash
ros2 topic echo /turtle1/cmd_vel
```

然后选中运行键盘控制节点的终端2，按方向键。

终端3会显示类似：

```yaml
linear:
  x: 2.0
  y: 0.0
  z: 0.0
angular:
  x: 0.0
  y: 0.0
  z: 0.0
---
```

向左或向右转动时，主要观察 `angular.z`的正负和大小。

使用 `Ctrl+C`停止观察。

### 2.8.2 观察海龟状态

```bash
ros2 topic echo /turtle1/pose
```

可能显示：

```yaml
x: 5.544445
y: 5.544445
theta: 0.0
linear_velocity: 0.0
angular_velocity: 0.0
---
```

各字段含义：

| 字段 | 含义 |
| --- | --- |
| `x` | 海龟横坐标 |
| `y` | 海龟纵坐标 |
| `theta` | 海龟朝向角 |
| `linear_velocity` | 当前线速度 |
| `angular_velocity` | 当前角速度 |

这说明 `/turtlesim`不仅订阅控制输入，也会发布自身状态。

从自动控制角度可以理解为：

- `/turtle1/cmd_vel`是控制输入 $u(t)$。
- `/turtle1/pose`是系统状态或输出 $x(t)$、$y(t)$。

---

## 2.9 从终端直接发布速度指令

键盘节点并不是海龟唯一的控制信号来源。只要按照正确的消息类型向 `/turtle1/cmd_vel`发布数据，海龟就能接收。

进行本实验时，可以先在键盘节点终端按 `Ctrl+C`停止 `/teleop_turtle`，避免两个发布者同时控制海龟。

### 2.9.1 只发布一次

```bash
ros2 topic pub --once /turtle1/cmd_vel geometry_msgs/msg/Twist "{linear: {x: 2.0}, angular: {z: 0.0}}"
```

这条命令表示：

| 内容 | 含义 |
| --- | --- |
| `ros2 topic pub` | 从终端创建临时发布者 |
| `--once` | 只发布一次消息 |
| `/turtle1/cmd_vel` | 目标话题 |
| `geometry_msgs/msg/Twist` | 消息类型 |
| `{...}` | 本次发布的具体数据 |

由于只发送一次，而海龟需要连续控制信号，实际移动距离可能很短。

### 2.9.2 持续发布

以每秒 `2`次的频率发布：

```bash
ros2 topic pub --rate 2 /turtle1/cmd_vel geometry_msgs/msg/Twist "{linear: {x: 1.5}, angular: {z: 0.8}}"
```

此时海龟会沿圆弧持续运动。

按下：

```text
Ctrl + C
```

停止发布。

### 2.9.3 常用运动组合

| `linear.x` | `angular.z` | 运动效果 |
| ---: | ---: | --- |
| `2.0` | `0.0` | 向前直行 |
| `-2.0` | `0.0` | 向后直行 |
| `0.0` | `1.0` | 原地向一个方向旋转 |
| `0.0` | `-1.0` | 原地向相反方向旋转 |
| `1.5` | `0.8` | 沿圆弧运动 |

这里的正负方向应以小海龟窗口中的实际现象为准，并在实验记录中写出观察结果。

---

## 2.10 测量话题发布频率

ROS 2中的传感器、定位和控制数据通常会周期性发布。频率单位为 Hz，表示每秒发布多少条消息。

观察海龟位姿话题频率：

```bash
ros2 topic hz /turtle1/pose
```

等待几秒后可能看到：

```text
average rate: ...
```

含义是该话题平均每秒发布多少条消息。

按 `Ctrl+C`停止测量。

也可以观察速度命令：

```bash
ros2 topic hz /turtle1/cmd_vel
```

但如果当前没有节点持续发布速度命令，终端可能暂时没有统计结果。这不代表命令出错，而是没有足够的消息可供测量。

---

## 2.11 一个话题可以有多个通信端点

话题不是只能连接一对节点。一个话题可以具有：

- 一个发布者和多个订阅者。
- 多个发布者和一个订阅者。
- 多个发布者和多个订阅者。

例如，执行：

```bash
ros2 topic echo /turtle1/pose
```

会临时创建一个订阅者，但 `/turtlesim`仍然可以继续发布位姿数据。

需要注意：如果多个发布者同时向 `/turtle1/cmd_vel`发送不同控制命令，海龟可能表现异常。真实机器人系统通常需要明确控制权，避免控制指令互相冲突。

---

## 2.12 话题、服务和动作的区别

| 通信方式 | 典型特点 | 小海龟示例 | 适用场景 |
| --- | --- | --- | --- |
| 话题 Topic | 连续发布，不要求逐条回复 | `/turtle1/cmd_vel` | 传感器、状态、控制流 |
| 服务 Service | 一次请求对应一次响应 | `/reset` | 查询或快速执行一次操作 |
| 动作 Action | 有目标、反馈、结果，可取消 | `/turtle1/rotate_absolute` | 耗时任务 |

本章只深入学习话题和消息，服务与动作将在后续章节单独学习。

---

## 2.13 常见问题

### 2.13.1 `ros2 topic echo`没有输出

可能原因：

1. 当前没有发布者发布消息。
2. 话题名称输入错误。
3. 速度话题只在按键时产生消息。
4. 对应节点已经被 `Ctrl+C`停止。

可以检查：

```bash
ros2 topic info <话题名称>
```

### 2.13.2 终端一直被命令占用

`ros2 topic echo`、`ros2 topic hz`和持续发布命令都需要一直运行。观察完成后使用：

```text
Ctrl + C
```

结束命令。

### 2.13.3 提示找不到话题或消息类型

首先检查 ROS 2环境：

```bash
echo $ROS_DISTRO
```

如果没有输出：

```bash
source /opt/ros/jazzy/setup.bash
```

然后检查话题名称和消息类型是否完整、大小写是否正确。

### 2.13.4 发布命令中的引号和括号报错

建议整段复制本章命令。消息数据外层使用英文双引号，字段名和花括号也必须使用英文符号。

---

## 2.14 本章实践任务

### 任务一：识别通信关系

对于下面的通信链：

```text
/teleop_turtle  -->  /turtle1/cmd_vel  -->  /turtlesim
```

写出：

1. 发布者节点。
2. 订阅者节点。
3. 话题名称。
4. 完整消息类型。
5. 主要使用的两个消息字段。

### 任务二：观察控制信号

使用：

```bash
ros2 topic echo /turtle1/cmd_vel
```

分别按上、下、左、右方向键，记录 `linear.x`和 `angular.z`的变化。

| 按键 | `linear.x` | `angular.z` | 实际运动 |
| --- | ---: | ---: | --- |
| 上 |  |  |  |
| 下 |  |  |  |
| 左 |  |  |  |
| 右 |  |  |  |

### 任务三：终端控制海龟

不用键盘节点，使用 `ros2 topic pub`完成：

1. 让海龟直行。
2. 让海龟原地旋转。
3. 让海龟沿圆弧运动。
4. 解释为什么改变 `linear.x`和 `angular.z`会得到不同轨迹。

### 任务四：控制系统映射

从自动控制角度说明：

- 哪个节点相当于控制器？
- 哪个节点相当于被控对象？
- 哪个话题相当于控制输入？
- 哪个话题可以作为状态反馈？

---

## 2.15 本章检查清单

完成本章后，应能独立完成以下项目：

- [ ] 能区分话题名称、消息类型和实际消息数据。
- [ ] 能解释发布者和订阅者的作用。
- [ ] 能使用 `ros2 topic list -t`查看话题和类型。
- [ ] 能使用 `ros2 topic info`查看端点数量。
- [ ] 能使用 `ros2 interface show`查看消息结构。
- [ ] 能使用 `ros2 topic echo`观察实时数据。
- [ ] 能使用 `ros2 topic pub`主动发布消息。
- [ ] 能使用 `ros2 topic hz`测量频率。
- [ ] 能将小海龟通信结构对应到自动控制框图。

---

## 2.16 本章核心总结

1. **话题**是带名称的数据通道，适合持续、异步地传输数据。
2. **发布者**产生数据，**订阅者**接收数据，同一个节点可以同时承担多种角色。
3. **消息类型**规定数据结构，**实际消息**是某一时刻传输的具体数值。
4. `/turtle1/cmd_vel`使用 `geometry_msgs/msg/Twist`，主要通过 `linear.x`和 `angular.z`控制二维运动。
5. `/turtle1/pose`发布海龟的位姿和速度，可理解为系统状态或输出。
6. ROS 2命令行工具既可以观察数据，也可以临时充当发布者或订阅者。

本章完成后，下一章将学习：**服务、参数与一次性请求/响应通信**。
