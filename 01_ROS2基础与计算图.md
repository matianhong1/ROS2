# 第1章 ROS 2基础与计算图

> 适用环境：Ubuntu 24.04 + ROS 2 Jazzy Jalisco  
> 学习方式：概念理解 → 命令观察 → 图形验证 → 独立练习  
> 本章定位：ROS 2入门基础，不要求编写完整功能包

---

## 1.1 本章学习目标

完成本章后，应能够：

1. 说明 ROS 2是什么，以及它解决了什么问题。
2. 理解节点（Node）、功能包（Package）、可执行程序（Executable）和计算图（Graph）。
3. 使用 `ros2 run` 启动一个 ROS 2程序。
4. 使用 `ros2 node list` 查看正在运行的节点。
5. 使用 `ros2 node info` 查看节点的通信接口。
6. 使用 `rqt_graph` 查看节点之间的连接关系。
7. 理解“程序、进程、节点”之间的基本关系。

---

## 1.2 ROS 2是什么

ROS 2全称为 **Robot Operating System 2**。虽然名称中包含“Operating System”，但它并不是 Windows、Ubuntu 这样的操作系统，而是一套用于开发机器人系统的软件框架和工具集合。

ROS 2主要解决以下问题：

- 将复杂机器人系统拆分为多个相对独立的模块。
- 让不同模块能够发现彼此并交换数据。
- 提供统一的消息、服务、动作和参数机制。
- 提供调试、可视化、数据记录和回放工具。
- 让 C++、Python 等不同语言编写的模块能够协同工作。

可以将 ROS 2理解为：

> ROS 2制定了一套机器人软件之间的通信规则，并提供了实现这些规则的工具。

### 1.2.1 自动驾驶系统示例

一个自动驾驶系统可以拆分成多个功能模块：

| 模块 | 主要任务 |
| --- | --- |
| 相机节点 | 获取道路图像 |
| 感知节点 | 识别车辆、行人和车道线 |
| 定位节点 | 估计车辆位置和姿态 |
| 规划节点 | 生成期望行驶轨迹 |
| 控制节点 | 计算油门、制动和转向指令 |
| 执行节点 | 将控制指令发送给车辆 |

如果所有功能都写进一个大型程序，任何模块发生修改都可能影响整个系统。ROS 2允许将它们拆成多个节点，每个节点只负责一类任务，再通过标准通信机制连接起来。

---

## 1.3 ROS 2的几个基础术语

### 1.3.1 功能包 Package

功能包是 ROS 2组织程序、配置文件和资源的基本单位。

一个功能包中可能包含：

- C++或 Python源代码
- 可执行程序
- Launch启动文件
- YAML参数文件
- 消息或服务接口
- `package.xml`依赖说明
- `CMakeLists.txt`编译规则

例如，小海龟程序所属的功能包叫作：

```text
turtlesim
```

### 1.3.2 可执行程序 Executable

可执行程序是功能包中能够直接运行的程序。

`turtlesim`功能包中包含多个可执行程序，例如：

```text
turtlesim_node
turtle_teleop_key
```

其中：

- `turtlesim_node`负责显示和更新海龟。
- `turtle_teleop_key`负责读取键盘并发送控制指令。

### 1.3.3 节点 Node

节点是 ROS 2系统中完成某项具体任务的运行单元。

例如：

| 节点名称 | 功能 |
| --- | --- |
| `/turtlesim` | 管理海龟的位置、姿态和图形显示 |
| `/teleop_turtle` | 读取键盘输入并生成速度指令 |

入门阶段可以暂时理解为：

> 运行一个 ROS 2可执行程序后，通常会创建一个节点。

但需要知道，更严格地说：

- 一个进程中可以包含一个节点。
- 一个进程中也可以包含多个节点。
- 节点是 ROS 2概念，进程是操作系统概念。

### 1.3.4 计算图 Graph

ROS 2计算图由以下内容共同组成：

- 当前正在运行的节点
- 节点发布或订阅的话题
- 节点提供或调用的服务
- 节点执行的动作
- 节点使用的参数
- 它们之间的连接关系

小海龟系统可以简化表示为：

```text
/teleop_turtle  --速度指令-->  /turtlesim
```

以后自动驾驶系统可能是：

```text
相机节点 --> 感知节点 --> 规划节点 --> 控制节点 --> 执行节点
```

---

## 1.4 `ros2 run`命令

运行 ROS 2程序的基本格式为：

```bash
ros2 run <功能包名称> <可执行程序名称>
```

例如：

```bash
ros2 run turtlesim turtlesim_node
```

这条命令可以分成三部分：

| 内容 | 含义 |
| --- | --- |
| `ros2 run` | 运行一个 ROS 2可执行程序 |
| `turtlesim` | 功能包名称 |
| `turtlesim_node` | 可执行程序名称 |

键盘控制程序的命令为：

```bash
ros2 run turtlesim turtle_teleop_key
```

两个命令使用了同一个功能包，但启动了不同的可执行程序。

### 1.4.1 查看功能包中的可执行程序

可以使用下面的命令查询某个功能包包含哪些可执行程序：

```bash
ros2 pkg executables turtlesim
```

也可以查询系统中的所有可执行程序：

```bash
ros2 pkg executables
```

后一条命令输出很多，入门阶段不需要逐项查看。

---

## 1.5 实操一：启动小海龟系统

### 1.5.1 启动海龟节点

打开终端1，运行：

```bash
ros2 run turtlesim turtlesim_node
```

正常现象：

- 弹出小海龟图形窗口。
- 终端保持运行状态，不会立即返回命令提示符。
- 这说明程序正在持续运行。

### 1.5.2 启动键盘控制节点

保持终端1和海龟窗口不关闭，再打开终端2：

```bash
ros2 run turtlesim turtle_teleop_key
```

保持终端2处于选中状态，使用键盘方向键控制海龟。

### 1.5.3 为什么需要两个终端

因为两个程序都需要持续运行：

- 终端1运行海龟节点。
- 终端2运行键盘控制节点。

如果在终端1按下 `Ctrl+C`，海龟节点就会停止；如果在终端2按下 `Ctrl+C`，键盘控制节点就会停止。

---

## 1.6 实操二：查看节点

保持前两个节点继续运行，打开终端3。

### 1.6.1 列出当前节点

```bash
ros2 node list
```

正常情况下会看到：

```text
/teleop_turtle
/turtlesim
```

节点名称前面的 `/`表示根命名空间。本阶段只需要认识这种写法，命名空间将在后续章节详细学习。

### 1.6.2 查看海龟节点的信息

```bash
ros2 node info /turtlesim
```

输出通常包含：

```text
Subscribers:
Publishers:
Service Servers:
Service Clients:
Action Servers:
Action Clients:
```

各部分含义如下：

| 项目 | 含义 |
| --- | --- |
| Subscribers | 该节点正在接收哪些话题数据 |
| Publishers | 该节点正在向外发布哪些话题数据 |
| Service Servers | 该节点提供哪些服务 |
| Service Clients | 该节点正在调用哪些服务 |
| Action Servers | 该节点提供哪些动作任务 |
| Action Clients | 该节点正在请求哪些动作任务 |

本章不要求记住所有接口，只需要认识：

> `ros2 node info`能够查看一个节点对外提供和使用的通信接口。

### 1.6.3 查看键盘控制节点

```bash
ros2 node info /teleop_turtle
```

将它与 `/turtlesim`的输出对比，可以初步发现：

- 键盘节点主要发送控制信息。
- 海龟节点接收控制信息，同时发布自身状态。

---

## 1.7 实操三：观察节点的出现和消失

1. 保持海龟节点运行。
2. 在键盘控制节点所在的终端按下 `Ctrl+C`。
3. 回到终端3，再次运行：

```bash
ros2 node list
```

此时 `/teleop_turtle`应该消失，只剩下：

```text
/turtlesim
```

这说明：

- 节点不是保存在一个固定列表里的名称。
- 节点只有在相应程序运行时才会出现在计算图中。
- 程序结束后，该节点会从计算图中消失。

重新启动键盘控制节点：

```bash
ros2 run turtlesim turtle_teleop_key
```

然后再次执行：

```bash
ros2 node list
```

`/teleop_turtle`会重新出现。

---

## 1.8 实操四：使用 `rqt_graph`查看计算图

在新的终端中运行：

```bash
rqt_graph
```

正常情况下会弹出一个窗口，显示节点和它们之间的连接关系。

如果图中内容不完整，可以：

1. 确认小海龟和键盘控制节点都在运行。
2. 单击窗口中的刷新按钮。
3. 在下拉选项中选择显示全部节点或话题。

观察重点：

- 找到 `/teleop_turtle`。
- 找到 `/turtlesim`。
- 观察两个节点之间是否存在连接。
- 暂时不要求记忆连接话题的名称。

`rqt_graph`展示的就是当前 ROS 2计算图。之后系统节点增多时，它是排查通信关系的重要工具。

---

## 1.9 实操五：修改节点名称

先停止原来的海龟节点，然后运行：

```bash
ros2 run turtlesim turtlesim_node --ros-args --remap __node:=my_turtle
```

再执行：

```bash
ros2 node list
```

正常情况下，原来的 `/turtlesim`会变成：

```text
/my_turtle
```

命令中的参数含义：

| 参数 | 含义 |
| --- | --- |
| `--ros-args` | 后面的内容属于 ROS 2参数 |
| `--remap` | 执行名称重映射 |
| `__node:=my_turtle` | 将节点名称修改为 `my_turtle` |

这说明节点名称不一定完全由程序固定，可以在启动时重新指定。

实验完成后按 `Ctrl+C`停止该节点，再重新使用默认命令启动：

```bash
ros2 run turtlesim turtlesim_node
```

---

## 1.10 C++节点代码预览

本章暂时不编译下面的代码，只用它观察一个节点大致由什么组成。创建工作空间和功能包之后，会逐行编写并运行类似程序。

```cpp
#include <memory>
#include "rclcpp/rclcpp.hpp"

int main(int argc, char * argv[])
{
    // 初始化 ROS 2
    rclcpp::init(argc, argv);

    // 创建一个名为 hello_node 的节点
    auto node = rclcpp::Node::make_shared("hello_node");

    // 在终端中输出日志
    RCLCPP_INFO(node->get_logger(), "Hello ROS 2!");

    // 让节点持续运行并处理事件
    rclcpp::spin(node);

    // 关闭 ROS 2
    rclcpp::shutdown();
    return 0;
}
```

现阶段只需要看懂下面三点：

1. `rclcpp::init()`用于初始化 ROS 2。
2. `make_shared("hello_node")`创建名为 `/hello_node`的节点。
3. `rclcpp::spin()`让节点保持运行并等待事件。

---

## 1.11 常用命令汇总

| 命令 | 功能 |
| --- | --- |
| `ros2 run <包名> <程序名>` | 运行 ROS 2可执行程序 |
| `ros2 pkg executables <包名>` | 查看功能包中的可执行程序 |
| `ros2 node list` | 列出当前正在运行的节点 |
| `ros2 node info <节点名>` | 查看节点的通信接口 |
| `rqt_graph` | 图形化显示 ROS 2计算图 |
| `Ctrl+C` | 停止当前终端中的程序 |

可以查看节点命令的帮助：

```bash
ros2 node --help
```

也可以查看 `node info`子命令的帮助：

```bash
ros2 node info --help
```

---

## 1.12 常见问题与易错点

### 问题1：`ros2 node list`没有输出

可能原因：当前没有 ROS 2节点运行。

解决方法：先运行小海龟节点，再执行查询命令。

### 问题2：提示 `ros2: command not found`

可能原因：ROS 2环境没有加载。

临时加载：

```bash
source /opt/ros/jazzy/setup.bash
```

如果已经将该命令写入 `~/.bashrc`，重新打开终端一般会自动加载。

检查当前 ROS 2版本：

```bash
echo $ROS_DISTRO
```

正常应输出：

```text
jazzy
```

### 问题3：方向键不能控制海龟

需要先用鼠标单击运行 `turtle_teleop_key`的终端，使该终端获得键盘焦点。

### 问题4：关闭终端后节点消失

这是正常现象。节点依赖对应程序运行，程序停止后节点就会退出计算图。

### 问题5：`rqt_graph`窗口中看不到连接

确认两个节点都处于运行状态，然后单击刷新按钮。某些调试选项还可能隐藏系统话题，需要调整显示选项。

---

## 1.13 本章练习

### 练习1：判断节点数量

按照以下顺序操作并记录每一步的节点数量：

1. 不运行任何海龟程序时执行 `ros2 node list`。
2. 只运行 `turtlesim_node`时执行 `ros2 node list`。
3. 同时运行 `turtlesim_node`和 `turtle_teleop_key`时执行 `ros2 node list`。

### 练习2：查询节点信息

分别执行：

```bash
ros2 node info /turtlesim
```

```bash
ros2 node info /teleop_turtle
```

思考：哪个节点负责发送控制信息，哪个节点负责接收控制信息？

### 练习3：自定义节点名称

将海龟节点改名为：

```text
/car_simulator
```

提示：参考本章第1.9节中的 `--remap __node:=...`。

### 练习4：用自己的话解释

尝试不用背定义，回答以下问题：

1. ROS 2为什么要把机器人程序拆成多个节点？
2. 功能包、可执行程序和节点有什么区别？
3. 为什么终止一个程序后，对应节点会从计算图中消失？

---

## 1.14 本章验收标准

能够独立完成以下任务，即可视为本章通过：

- [ ] 独立启动小海龟节点和键盘控制节点。
- [ ] 使用 `ros2 node list`找到两个节点。
- [ ] 使用 `ros2 node info`查看一个节点的信息。
- [ ] 使用 `rqt_graph`看到节点之间的连接。
- [ ] 停止一个程序，并观察对应节点从列表中消失。
- [ ] 使用重映射参数修改海龟节点的名称。
- [ ] 能够用自己的话解释什么是节点和计算图。

---

## 1.15 本章总结

本章最核心的知识可以概括为：

1. ROS 2是一套用于开发机器人系统的软件框架和通信工具。
2. 复杂系统可以拆分为多个节点，每个节点负责一种相对独立的功能。
3. 正在运行的节点及其通信关系组成 ROS 2计算图。
4. 功能包用于组织程序，可执行程序启动后通常会创建节点。
5. `ros2 node list`、`ros2 node info`和 `rqt_graph`可以用于观察计算图。

下一章将学习节点之间最常用的通信方式：**话题（Topic）与消息（Message）**。

