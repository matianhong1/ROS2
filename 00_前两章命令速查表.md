# ROS 2前两章命令速查表

> 适用环境：Ubuntu 24.04 + ROS 2 Jazzy Jalisco  
> 对应章节：第1章“ROS 2基础与计算图”、第2章“话题与消息”  
> 使用方法：先根据目标找到命令类别，再替换通式中的尖括号内容

---

## 1. 命令书写规则

笔记中的尖括号表示需要替换的内容，不要原样输入。

例如通式：

```bash
ros2 node info <节点名称>
```

查看海龟节点时，应写成：

```bash
ros2 node info /turtlesim
```

常见占位符：

| 占位符 | 含义 | 示例 |
| --- | --- | --- |
| `<功能包名称>` | ROS 2功能包名称 | `turtlesim` |
| `<可执行程序名称>` | 功能包中可运行的程序 | `turtlesim_node` |
| `<节点名称>` | 计算图中的节点名称 | `/turtlesim` |
| `<话题名称>` | 节点之间的数据通道 | `/turtle1/cmd_vel` |
| `<消息类型>` | 消息的完整类型名称 | `geometry_msgs/msg/Twist` |
| `<消息数据>` | 本次发送的具体字段和值 | `{linear: {x: 2.0}}` |

---

## 2. ROS 2接口名称的层级

ROS 2接口的完整名称通常采用：

```text
<功能包名称>/<接口类别>/<接口名称>
```

例如：

```text
geometry_msgs/msg/Twist
```

拆分如下：

| 层级 | 内容 | 解释 |
| --- | --- | --- |
| 功能包 | `geometry_msgs` | 几何消息功能包，提供常用几何数据类型 |
| 接口类别 | `msg` | 表示这是用于话题通信的消息接口 |
| 接口名称 | `Twist` | 描述线速度和角速度的消息类型 |

接口类别包括：

| 类别 | 英文 | 用途 | 示例 |
| --- | --- | --- | --- |
| `msg` | message | 话题消息 | `geometry_msgs/msg/Twist` |
| `srv` | service | 服务请求与响应 | `turtlesim/srv/Spawn` |
| `action` | action | 动作目标、反馈与结果 | `turtlesim/action/RotateAbsolute` |

### 2.1 `Twist`消息的嵌套结构

```text
Twist
├── linear：Vector3
│   ├── x：float64
│   ├── y：float64
│   └── z：float64
└── angular：Vector3
    ├── x：float64
    ├── y：float64
    └── z：float64
```

对应的C++结构体逻辑可以理解为：

```cpp
struct Vector3
{
    double x;
    double y;
    double z;
};

struct Twist
{
    Vector3 linear;
    Vector3 angular;
};
```

小海龟主要使用：

```text
linear.x   前进或后退的线速度
angular.z  向左或向右旋转的角速度
```

必须区分：

| 概念 | 示例 | 回答的问题 |
| --- | --- | --- |
| 话题名称 | `/turtle1/cmd_vel` | 数据通过哪条通道传输？ |
| 消息类型 | `geometry_msgs/msg/Twist` | 数据采用什么结构？ |
| 消息字段 | `linear.x` | 数据结构中的哪一项？ |
| 实际数据 | `linear.x = 2.0` | 这次实际传输了什么值？ |

---

## 3. 终端与环境命令

### 3.1 打开终端

```text
Ctrl + Alt + T
```

### 3.2 Ubuntu终端复制和粘贴

```text
复制：Ctrl + Shift + C
粘贴：Ctrl + Shift + V
```

### 3.3 停止当前正在运行的程序

```text
Ctrl + C
```

该快捷键只停止当前终端中的前台程序，不会自动停止其他终端中的节点。

### 3.4 检查ROS 2发行版

**用途：** 判断ROS 2环境是否已经加载。

**通式：**

```bash
echo $ROS_DISTRO
```

**当前环境的正常输出：**

```text
jazzy
```

### 3.5 手动加载ROS 2环境

**用途：** 当 `echo $ROS_DISTRO`没有输出时，手动加载Jazzy环境。

```bash
source /opt/ros/jazzy/setup.bash
```

当前电脑已经把该命令写入 `~/.bashrc`，正常情况下新终端会自动加载。

---

## 4. 运行ROS 2程序

### 4.1 运行功能包中的可执行程序

**用途：** 启动一个ROS 2程序。

**通式：**

```bash
ros2 run <功能包名称> <可执行程序名称>
```

**启动海龟仿真：**

```bash
ros2 run turtlesim turtlesim_node
```

**启动键盘控制：**

```bash
ros2 run turtlesim turtle_teleop_key
```

**逻辑关系：**

```text
功能包 turtlesim
├── 可执行程序 turtlesim_node
└── 可执行程序 turtle_teleop_key
```

### 4.2 查看功能包包含的可执行程序

**通式：**

```bash
ros2 pkg executables <功能包名称>
```

**示例：**

```bash
ros2 pkg executables turtlesim
```

### 4.3 运行时修改节点名称

**通式：**

```bash
ros2 run <功能包> <可执行程序> --ros-args --remap __node:=<新节点名称>
```

**示例：**

```bash
ros2 run turtlesim turtlesim_node --ros-args --remap __node:=car_simulator
```

此时：

| 项目 | 内容 |
| --- | --- |
| 功能包 | `turtlesim` |
| 可执行程序 | `turtlesim_node` |
| 运行后的节点名称 | `/car_simulator` |

这证明功能包、可执行程序和节点不是同一个概念。

---

## 5. 节点命令

### 5.1 列出当前正在运行的节点

**用途：** 回答“ROS 2系统中现在有哪些节点？”

**通式：**

```bash
ros2 node list
```

**示例输出：**

```text
/teleop_turtle
/turtlesim
```

### 5.2 查看节点的通信接口

**用途：** 查看节点发布、订阅的话题，以及服务和动作接口。

**通式：**

```bash
ros2 node info <节点名称>
```

**查看海龟节点：**

```bash
ros2 node info /turtlesim
```

**查看键盘控制节点：**

```bash
ros2 node info /teleop_turtle
```

**查看重命名后的节点：**

```bash
ros2 node info /car_simulator
```

输出栏目：

| 栏目 | 含义 |
| --- | --- |
| `Subscribers` | 该节点订阅哪些话题 |
| `Publishers` | 该节点向哪些话题发布消息 |
| `Service Servers` | 该节点提供哪些服务 |
| `Service Clients` | 该节点调用哪些服务 |
| `Action Servers` | 该节点提供哪些动作 |
| `Action Clients` | 该节点请求哪些动作 |

---

## 6. 计算图命令

### 6.1 打开计算图工具

```bash
rqt_graph
```

**用途：** 用图形观察节点与话题之间的连接。

基本图形含义：

| 图形 | 含义 |
| --- | --- |
| 椭圆 | 节点 |
| 矩形 | 话题 |
| 箭头 | 发布或订阅方向 |

小海龟控制链：

```text
/teleop_turtle → /turtle1/cmd_vel → /turtlesim
```

---

## 7. 话题查询命令

### 7.1 列出话题名称

```bash
ros2 topic list
```

**用途：** 查看当前计算图中有哪些话题。

### 7.2 同时列出话题和消息类型

```bash
ros2 topic list -t
```

**推荐使用这一版本。**

示例输出：

```text
/turtle1/cmd_vel [geometry_msgs/msg/Twist]
/turtle1/pose [turtlesim/msg/Pose]
```

其中：

- 方括号前面是话题名称。
- 方括号里面是完整消息类型。

### 7.3 查看话题的连接信息

**通式：**

```bash
ros2 topic info <话题名称>
```

**示例：**

```bash
ros2 topic info /turtle1/cmd_vel
```

示例输出：

```text
Type: geometry_msgs/msg/Twist
Publisher count: 1
Subscription count: 1
```

解释：

| 输出 | 含义 |
| --- | --- |
| `Type` | 话题使用的消息类型 |
| `Publisher count` | 当前发布者数量 |
| `Subscription count` | 当前订阅者数量 |

数量表示通信端点数量，不表示已经发送了多少条消息。

### 7.4 查看详细端点信息

**通式：**

```bash
ros2 topic info <话题名称> --verbose
```

**示例：**

```bash
ros2 topic info /turtle1/cmd_vel --verbose
```

该命令还会显示节点名称、命名空间和QoS。本阶段只要求认识，不要求记忆QoS细节。

---

## 8. 接口结构查询命令

### 8.1 查看消息内部字段

**通式：**

```bash
ros2 interface show <完整接口类型>
```

**查看速度消息：**

```bash
ros2 interface show geometry_msgs/msg/Twist
```

**查看三维向量：**

```bash
ros2 interface show geometry_msgs/msg/Vector3
```

**查看海龟位姿消息：**

```bash
ros2 interface show turtlesim/msg/Pose
```

该命令回答：

> 这种消息内部有哪些字段，每个字段是什么数据类型？

---

## 9. 监听话题命令

### 9.1 持续监听话题

**通式：**

```bash
ros2 topic echo <话题名称>
```

**监听速度指令：**

```bash
ros2 topic echo /turtle1/cmd_vel
```

**监听海龟位姿：**

```bash
ros2 topic echo /turtle1/pose
```

持续监听时使用 `Ctrl+C`结束。

### 9.2 只读取一条消息

**通式：**

```bash
ros2 topic echo --once <话题名称>
```

**示例：**

```bash
ros2 topic echo --once /turtle1/pose
```

`--once`表示收到一条消息后自动退出。

---

## 10. 发布话题命令

### 10.1 发布命令的完整通式

```bash
ros2 topic pub <发布选项> <话题名称> <消息类型> "<消息数据>"
```

四个关键部分：

| 部分 | 作用 |
| --- | --- |
| `ros2 topic pub` | 创建一个命令行临时发布者 |
| `<发布选项>` | 决定发布一次还是持续发布 |
| `<话题名称>` | 消息发送到哪条通道 |
| `<消息类型>` | 消息必须满足什么结构 |
| `<消息数据>` | 本次发送的字段和值 |

### 10.2 只发布一次

**通式：**

```bash
ros2 topic pub --once <话题名称> <消息类型> "<消息数据>"
```

**让海龟向前移动一小段：**

```bash
ros2 topic pub --once /turtle1/cmd_vel geometry_msgs/msg/Twist "{linear: {x: 2.0}, angular: {z: 0.0}}"
```

### 10.3 按固定频率持续发布

**通式：**

```bash
ros2 topic pub --rate <频率> <话题名称> <消息类型> "<消息数据>"
```

**以2 Hz让海龟沿圆运动：**

```bash
ros2 topic pub --rate 2 /turtle1/cmd_vel geometry_msgs/msg/Twist "{linear: {x: 1.5}, angular: {z: 0.8}}"
```

使用 `Ctrl+C`停止持续发布。

### 10.4 常用控制数据

| `linear.x` | `angular.z` | 运动效果 |
| ---: | ---: | --- |
| `2.0` | `0.0` | 向前直行 |
| `-2.0` | `0.0` | 向后直行 |
| `0.0` | `2.0` | 原地向左旋转 |
| `0.0` | `-2.0` | 原地向右旋转 |
| `1.5` | `0.8` | 沿圆弧运动 |

当线速度和角速度保持不变且均不为0时，理论转弯半径为：

$$
R=\frac{v}{\omega}
$$

---

## 11. 测量话题频率

### 11.1 测量消息到达频率

**通式：**

```bash
ros2 topic hz <话题名称>
```

**示例：**

```bash
ros2 topic hz /turtle1/cmd_vel
```

常见输出：

```text
average rate: 4.9
min: 0.19s max: 0.22s std dev: ... window: ...
```

解释：

| 输出 | 含义 |
| --- | --- |
| `average rate` | 平均每秒收到多少条消息 |
| `min` | 相邻消息的最短时间间隔 |
| `max` | 相邻消息的最长时间间隔 |
| `std dev` | 消息间隔的波动程度 |
| `window` | 统计所使用的消息数量 |

频率和周期的关系：

$$
T=\frac{1}{f}
$$

例如5 Hz对应约0.2 s的周期。

---

## 12. 前两章最重要的命令

现阶段至少要知道以下命令分别解决什么问题：

```bash
ros2 run <功能包> <可执行程序>
ros2 node list
ros2 node info <节点名称>
ros2 topic list -t
ros2 topic info <话题名称>
ros2 interface show <完整接口类型>
ros2 topic echo <话题名称>
ros2 topic pub <发布选项> <话题名称> <消息类型> "<消息数据>"
ros2 topic hz <话题名称>
rqt_graph
```

不要求脱离笔记默写所有长命令，但必须知道遇到问题时应该使用哪一类命令。

---

## 13. 推荐的系统排查顺序

面对一个陌生ROS 2系统时，可以按照以下顺序调查：

### 第一步：有哪些节点？

```bash
ros2 node list
```

### 第二步：节点具有什么接口？

```bash
ros2 node info <节点名称>
```

### 第三步：有哪些话题和消息类型？

```bash
ros2 topic list -t
```

### 第四步：话题两端连接了多少节点？

```bash
ros2 topic info <话题名称>
```

### 第五步：消息内部有什么字段？

```bash
ros2 interface show <消息类型>
```

### 第六步：话题实际传输了什么数据？

```bash
ros2 topic echo <话题名称>
```

核心思路：

```text
先看节点 → 再看话题 → 再看类型 → 最后看实际数据
```

---

## 14. 小海龟完整实验命令顺序

### 终端1：启动海龟

```bash
ros2 run turtlesim turtlesim_node
```

### 终端2：启动键盘控制

```bash
ros2 run turtlesim turtle_teleop_key
```

### 终端3：观察系统

```bash
ros2 node list
ros2 node info /turtlesim
ros2 topic list -t
ros2 topic info /turtle1/cmd_vel
ros2 interface show geometry_msgs/msg/Twist
ros2 topic echo /turtle1/cmd_vel
```

停止 `echo`后，可以读取一条位姿：

```bash
ros2 topic echo --once /turtle1/pose
```

停止键盘节点后，可以由终端直接控制海龟：

```bash
ros2 topic pub --rate 2 /turtle1/cmd_vel geometry_msgs/msg/Twist "{linear: {x: 1.5}, angular: {z: 0.8}}"
```

---

## 15. 学习检查清单

- [ ] 能解释功能包、可执行程序和节点的区别。
- [ ] 能解释发布者、话题和订阅者的关系。
- [ ] 能拆解 `geometry_msgs/msg/Twist`的三层结构。
- [ ] 能区分话题名称、消息类型、消息字段和实际数据。
- [ ] 知道如何列出节点和话题。
- [ ] 知道如何查看节点与话题的详细信息。
- [ ] 知道如何查看消息结构。
- [ ] 知道如何监听和发布话题数据。
- [ ] 知道如何测量话题频率。
- [ ] 能按照“节点→话题→类型→数据”的顺序调查陌生系统。

